import Calendar from '../../../../dashboard_ot/components/calendar';

const ScheduleTab = () => {
  return (
    <div style={{ paddingInline: '2rem', paddingBlock: '4rem' }}>
      <Calendar />
    </div>
  );
};

export default ScheduleTab;
